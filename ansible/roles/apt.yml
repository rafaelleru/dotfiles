# file: dotfiles.yml
---
- name: Install apt packages
  apt:
    name: "{{ item }}"
    state: present
  loop:
    - jq
    - curl
    - htop
    - python3
    - python3-dev
    - docker-compose
    - docker
    - git
    - dmenu
    - make
    - gcc
    - ninja-build
    - xorg 
    - libx11-dev 
    - libxft-dev
    - libxinerama-dev
    - xdm
    - libxfixes-dev
    - xsel
    - cmake
    - g++
    - libtool-bin
    - libblis64-pthread-dev
    - gettext
    - ripgrep
